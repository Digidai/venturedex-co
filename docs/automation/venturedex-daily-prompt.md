只执行一次有界的 VentureDex Daily curation 或一个已中断 Daily 的恢复 closeout；不得在同一任务中启动第二轮 discovery。

开始时先检查 `$CODEX_HOME/automations/venturedex-daily-curator/run-state.md`、同目录的 `run-state.lease.json`、`git worktree list --porcelain`、严格按 run id/worktree 筛选的相关进程、近期 `origin/main` 提交和中央 GSC ledger。如果恰好有一个可证明归属的未完成 Daily，进入其 exact worktree，按最后一个有持久证据的 phase 恢复；不要复用旧 exec cell、tool-call id 或 PID，也不要重复 discovery、commit、deploy 或可能已经发生的 GSC click。若存在多个可能归属的 dirty worktree、SHA 冲突或无法证明所有权，立即 fail closed 并报告 exact paths。不要把未过滤的全局进程 argv 载入会话。

如果没有未完成运行，先只更新 Git refs，从 exact current `origin/main` 创建 detached Daily worktree，记录 `RUN_WORKTREE`，保持 `/Users/dai/Developer/CursorProjects/venturedex.co` 主 checkout 完全只读。在 selected worktree 内、bootstrap/discovery 之前运行 `python3 scripts/automation-run-state.py acquire --run-id "$RUN_ID"`，保留返回的 lease epoch 和 checkpoint revision；正常 owner 来自 `CODEX_THREAD_ID`，不得用 run id、PID 或共享常量冒充 owner。不同活跃 owner 立即 fail closed。只有 heartbeat 超过 6 小时、恢复的是同一 run id、提供 exact expected epoch 且现场证据证明没有匹配进程仍在改动 worktree 时，才允许 stale takeover。

所有 run-state 更新必须调用 `scripts/automation-run-state.py checkpoint` 并传入当前 epoch 与 exact expected revision，随后携带返回的新 revision；不得手改 `run-state.md` 或 `run-state.lease.json` 绕过 CAS。checkpoint 只包含 run id、UTC 时间、worktree、base/current/pushed SHA、phase、accepted slugs 和 blocker，不得包含凭据、raw thread identity、私有数据或整页来源内容。至少在 preflight、discovery、content_prepared、local_gates_passed、pushed、deployed、gsc 和 closeout 后更新一次。

在 selected Daily worktree 内，先阅读 `docs/automation/README.md`，再按其可信顺序阅读 `content/STANDARD.md`、`content/CODEX_TASK.md`、`docs/automation/venturedex-daily-runbook.md`、`docs/automation/venturedex-feedback-loop.md`、`docs/automation/venturedex-learning-log.md`、`docs/newsletter.md` 和 `docs/observability.md`。Learning log 读取模板/表头和最近 10 条；只有定位一个具体重复错误时才定向搜索更早记录，不要每次把整份 append-only 历史载入上下文。如任何必需 Markdown 缺失或相互不一致，停止并报告，切勿猜测。

候选发现前必须在 selected Daily worktree 执行 `./scripts/bootstrap-automation.sh venturedex-daily-curator`。它只作用于当前 checkout，因此必须先进入 detached worktree。Bootstrap 负责恢复 repo-local `.env`、校验 Cloudflare token、确认 GitHub Actions、检查依赖；任何失败都是 discovery 前 hard stop，不得继续 rejected-only/no-op 兜底。

当前架构以 repo control plane 为准：JSON-first、Astro 7、Cloudflare adapter 14。`content/*.json` 通过 `src/lib/content-transform.ts` prerender，`scripts/build-db.sh` 生成 D1 seed，`tests/content-parity.test.ts` 防止转换链漂移。新增 startup 必须同步 `content/timestamps.json`，用 UTC `YYYY-MM-DD HH:MM:SS` 记录新 slug 的 `published_at` 和 `first_seen_at`。

新 Daily 先读 docs/automation/curation-decisions.md 和 funding-terms.md。运行 npm run curation:plan 选最多 3 个到期复审，再用近 30 天融资信号补足同一个 10-20 家唯一公司的固定池；复审和 fresh 合计占池容量，不另起 discovery。至少尝试三类互补的公司/投资机构原始公告、原创媒体、地区媒体、行业媒体或研究来源，记录查询及未找到的结果，聚合器只作线索，不局限英语/美元/美国媒体。先用 curation:lookup 查有效 overlay 再查旧拒绝，保存 content/curation-runs/{run_id}.json 和 locked identity hash 后才评分。最多新增 5 家，无拒绝数量/比例或收录率目标；不得为凑比例追加候选，合格溢出保留 qualified_pending，清晰 no-op 合法。

60 秒只作分流，不把访问失败、缺少证据、原币种、扩展轮次或未披露 lead 当作低质量。按行业看真实流程、API/文档、企业部署、硬件规格/测试、公开医疗验证；官网模板与销售门槛不代表产品质量。来源自述不能写成独立证实。按八状态记录全部候选，更新 content/curation-reviews.json；访问/证据/schema/合格溢出/发布阻塞与质量拒绝/政策排除分开。已有历史行用 exact SHA-256 绑定，保留冻结账本和摘要；不得改写历史来纠错。每次实际复查保留 attempts，访问失败 1-7 天、其他 pending 1-30 天后复查；不当日盲重试，不把规划当研究。只有真实 startup 与全部门禁通过才写 accepted。新确证质量/政策否决可另写完整 v2 历史行并绑定，不为 pending 追加 rejection。

融资支持 Pre-Seed、Seed、Pre-Series A、具名 Series A-Z，D+ 保留原有 breakout exception。stage_raw 保留来源的 +/extension，currency 保留 ISO 4217 原币种，instrument 区分 equity/debt/mixed/grant/undisclosed；不猜 stage、汇率、股债分配或 lead。金额/lead 未披露使用 undisclosed；未知具名阶段先 evidence_pending，不能猜值或永久拒绝。严格遵守所有 immutable guards。每个新增 startup 必须有结构化 `research`：官方来源、融资来源、至少两条 source-linked 产品证据、market_context 和一个可验证风险/开放问题。品牌素材必须来自官方来源并登记到 `content/brand-assets.json`。官网、官方 ATS 或明确官方 company jobs page 暴露 Careers/Jobs/Open Roles 时，写静态 `links.careers`；不抓岗位列表、数量、地点、薪资或招聘断言。不要修改 Weekly。

产品试用、网页核验、截图、登录态和 GSC 统一使用 Codex 内置浏览器（CUA，browser: iab）。先阅读 docs/automation/gsc-codex-browser.md。只操作本次创建的标签页，每个动作基于新读取的可见页面；只关闭自己的标签，不操作用户标签或其他浏览器进程。禁止依赖或退回 bb-browser、Comet/Chrome CDP 或共享 daemon；Codex 浏览器不可用时记录具体 blocker。截图必须按 docs/automation/screenshot-quality.md 执行：使用至少 1280×720、默认缩放的 native capture，等待字体、动画和产品内容稳定，真实 UI 关闭遮挡，再以 scripts/screenshot.sh <slug> <url> --from-codex <absolute-image> --reviewed 离线导入。导入不等于验收。由与 capture_operator 不同的真实 reviewer 检查最终 WebP、实际卡片和自然比例详情图，完成 loaded/unobstructed/legible/framing/card/detail 六项，使用 scripts/screenshot-quality.mjs approve 按 exact SHA-256 写入 content/screenshot-reviews.json；不得虚构第二 reviewer 或把未检查项设为 true。任何换图都会使旧审批失效；不合格必须重拍，不得改用 historical-reviewed 或修改冻结的 screenshot-history-baseline.json 绕过审核。必须把图片与对应 review ledger 一起提交，screenshots:validate 和 build 均为 hard gate。

出现错误时按 runbook 的研究迭代环定位具体命令、文件、日志、最近学习和一手来源，先给出 root cause/blocker class，再做允许范围内的最小修复并重跑失败步骤及下游门禁。不要盲重试。传输层 `request timed out` 或 `stream disconnected` 不等于 repo gate 失败；从 run-state、filesystem、Git、CI、deploy、GSC ledger/artifacts 恢复。

发布前必须依次通过 `./scripts/check-github-actions.sh`、`./scripts/manage.sh validate` 和 `git diff --check`。完整 gate 包含 dependency audit、content validation、deterministic D1 seed、newsletter/unit tests、Astro sync、TypeScript 和 Astro build。单独的 validate/build-db/build 仅用于定位。`d1/generated-seed.sql`、`.astro/`、`.playwright-cli/`、`scripts/__pycache__/` 等验证/缓存输出不得提交。

只有 runbook 允许且所有本地门禁通过时才提交并 push 到 `origin/main`。内容和 automation-doc 分开 commit。Push 后记录 exact SHA，等待同 SHA Validate/Deploy，必要时仅按 runbook 使用 release/smoke 兜底，并做 live smoke。不要自动回滚 main。

部署和 live smoke 后，运行 python3 scripts/gsc-codex.py plan --latest-daily，只读确认目标仅为本次 /startups/{slug}。按照 docs/automation/gsc-codex-browser.md，在 Codex 内置浏览器中检查 exact URL；保存脱敏最小证据，先执行 begin --url URL --evidence FILE 持久化 intent，成功返回后 60 秒内只点一次 Request indexing，再以新观察执行 finish --attempt ID --evidence FILE。中央 $CODEX_HOME/automations/venturedex-daily-curator/gsc_submission_history.tsv 是权威 ledger。requested URL 跳过；只有明确 URL-bound 成功确认才记 requested，requested 不等于已索引。配额停止整个批次；任何待确认点击、孤立 intent 或 post_request_confirmation_unknown 均不得重新点击、force 或普通 retry。未点击的剩余 URL 保持待处理并记录 blocker，不运行旧 shell mutation。

不要手动触发 newsletter。Daily 邮件由 Cloudflare Cron 在发布后的延迟窗口处理，状态以 D1 `newsletter_sends` / `newsletter_deliveries` 为准。

运行结束必须追加 learning log、更新 automation memory，并让最终任务响应作为 inbox closeout，完整列出内容、奖励、验证、commit/push/deploy、GSC、newsletter 延迟、transport recovery、worktree cleanup 和启发式变更。只在明确 auto-edit 区域内做 automation self-edit；超出范围的策略记 deferred。

Detached worktree 只有在 commit/push、deploy/GSC、learning log、automation memory 和 inbox/closeout 证据全部持久化后才能清理。先以 `active/closeout` checkpoint 持久化当前 revision，记录 `RUN_WORKTREE`，切回主 checkout，运行 `bash scripts/cleanup-automation-worktrees.sh --execute --path "$RUN_WORKTREE"`。Dirty、unreachable 或所有权不明时不得强删；worktree 仍在时用其中 helper checkpoint `blocked` 并记录 exact path/files，再 release 该 terminal blocked lease。只有确认路径 absent/unregistered 后，才从 exact pushed Git SHA 读取并校验 `scripts/automation-run-state.py`（不要依赖 stale main checkout），以相同 owner/epoch/revision checkpoint `complete`，然后 release terminal lease。任何 helper blob、CAS 或 release 失败都必须如实报告，不能把 closeout 写成完成。

历史 terminal blocked worktree 不得长期积累为下一轮 preflight 债务。若 exact checkpoint 为 blocked/closeout、同 epoch lease 已 released、无 exact matching process、HEAD 可从 origin ref 到达，且唯一 dirty path 是 docs/automation/venturedex-learning-log.md，则先持久化 automation memory 和 inbox closeout，再运行 scripts/archive-automation-worktree-evidence.sh 的 dry-run，使用它返回的 exact HEAD 与 status SHA-256 执行归档。只有外部 Git bundle 和 JSON manifest 验证成功、worktree 已 clean 后，才可用普通 cleanup helper（不得 --force-dirty）删除。历史 checkpoint 保持 blocked；不得为美化状态重标 complete。任何 startup/rejection/asset/screenshot/额外 dirty path、active lease、匹配进程、CAS 漂移或所有权不明仍立即 fail closed。

GSC 在 begin 之前因登录/浏览器不可用而阻塞，或配额发生后仍有从未点击的余项时，按原计划的 exact URL 逐个使用 python3 scripts/gsc-codex.py defer --url URL --reason REDACTED_SINGLE_LINE 保存 retry_pending；不得用于任何已开始、已点击、已 requested 或 unknown 目标。已有不可变结果回执落盘而 ledger 未完成时，仅使用 recover --attempt ID 零浏览器恢复，不能再次点击。

人类新增要求（2026-09-09）：为本次准备新增或更新的 startup 所关联的投资机构维护可溯源资料。先精确匹配 canonical directory，合并本次所有明确参投方与领投方并去重；机构实体、品牌别名、共同领投不能混为一谈。阅读 docs/automation/investor-research.md，然后运行 npm run investors:plan -- --startup <slug>（可重复 --startup；内容准备前可用 --investor <canonical-slug>）。已有完整资料距 reviewed_at 不满 90 天且无实质变化则跳过，日期和文字均不重写；满 90 天再复查官方来源；新机构必须补充最小可用、逐字段有来源的结构化档案，既有缺档案机构按本次关联范围补齐。核实重组、更名、投资策略变化、主体差异或事实错误时可提前复查，并记录具体来源。不要把常规新融资提及当作机构资料需要全文重写的理由，不要每次扫描整个目录。检查 unresolved_investors 与 identity_warnings，尤其是复合投资方字符串及 Lightspeed/LSIP，不能为了通过校验而猜别名。未知字段省略；查询失败只记录 1–7 天 retry 与 exact URL，保留旧 reviewed_at，不能伪装已更新。新机构不得放入 legacy_unresearched 豁免；必要证据无法获得时如实 defer。全部完成后运行 npm run investors:validate，再执行原有完整门禁，并在现有 learning entry 记录 researched、fresh-skipped、retry-deferred、identity-unresolved 数量。

治理修复生效边界（2026-09-09）：当前 main 应具备 curation review/manifest、funding terms 和 investor profile 工具。旧未发布 Daily 的工作树若仍停留在修复前版本，必须先保全 exact diff/截图/旧池证据，并按人类批准的治理升级恢复，不能用旧 3:1 或旧 schema 发布，也不能把旧草稿拒绝未经复审直接提交。不得自行改冻结摘要或凭空生成独立截图审核；所有权或安全迁移不能证明时保留原运行并报告 exact blocker，不另起一轮。每轮 learning entry 按八状态和唯一候选分母报告，同时记录 source coverage、到期积压/最老日期、实际复审数及纠正数，不用拒绝数量作为奖励。机构维护记录 researched、fresh-skipped、retry-deferred、identity-unresolved；只有实际部署和 live 检查完成才称页面上线。
