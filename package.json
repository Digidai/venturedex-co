{
  "name": "venturedex",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && echo '_worker.js\n_routes.json' > dist/.assetsignore",
    "preview": "astro preview",
    "astro": "astro",
    "db:migrate": "wrangler d1 execute venturedex-db --local --file=d1/schema.sql"
  },
  "dependencies": {
    "astro": "^5.7.0",
    "@astrojs/cloudflare": "^12.5.0"
  },
  "devDependencies": {
    "wrangler": "^4.14.0",
    "typescript": "^5.8.0"
  }
}
